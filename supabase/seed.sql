-- ============================================================
-- Seed Data — Demo agents for testing
-- ============================================================
-- Note: In production, agents are created via Supabase dashboard
-- after auth user registration.

-- These inserts assume corresponding auth.users exist.
-- For local development, create users via Supabase Auth dashboard first.

-- Example:
-- INSERT INTO agents (id, full_name, email, phone, company)
-- VALUES (
--   'UUID-from-supabase-auth',
--   'Danny',
--   'danny@elevate.co.uk',
--   '07700 900000',
--   'Elevate'
-- );
